package com.beleningalina.jetpackcomposepoc.ui.theme

import androidx.compose.ui.graphics.Color
object Colors {
    // El tono principal de cerceta/cian de la imagen, saturado
    val VibrantTeal = Color(0xFF00C4A1)

    // Un tono de cerceta más oscuro y suave para el fondo, derivado de la imagen
    val MutedTealBackground = Color(0xFF027A71)

    // Un tono más claro del mismo cian para usar como acento o fondo secundario
    val LightTeal = Color(0xFF66E0CF)

    // Para contraste de texto
    val TextOnTeal = Color.White
    val TextOnLightTeal = Color(0xFF015A52) // Un tono muy oscuro de la misma gama

    // Color de superficie genérico (como el blanco de la imagen, pero más suave)
    val SurfaceWhite = Color(0xFFFBFBFB)
}